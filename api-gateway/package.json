{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway (Client API) service for photo management",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "build": "npm run clean && npm run type-check && tsc",
    "start": "node dist/index.js",
    "dev": "nodemon --watch \"src/**/*.ts,index.ts\" --exec \"ts-node --env-file=.env\" index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "multer": "^2.0.2",
    "uuid": "^9.0.1",
    "@shared-infra/event-bus": "*",
    "@shared-infra/job-queue": "*",
    "@shared-infra/storage-client": "*"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.7.2",
    "@types/uuid": "^9.0.1",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
