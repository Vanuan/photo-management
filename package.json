{
  "name": "photo-management-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api-gateway",
    "shared-infra",
    "shared-infra/packages/*",
    "worker",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install",
    "build-all": "npm run build -ws",
    "type-check-all": "npm run type-check -ws"
  }
}
