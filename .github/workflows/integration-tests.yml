name: DISABLED - Integration Tests (use e2e.yml)

on:
  workflow_dispatch:

jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/e2e.yml for E2E and integration testing"